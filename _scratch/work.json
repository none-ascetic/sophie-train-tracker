{"2026-06-23": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-06-23T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-06-23T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-06-23T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-06-30": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-06-30T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-06-30T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-06-30T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-07-07": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-07-07T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-07-07T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-07-07T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-07-14": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-07-14T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-07-14T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-07-14T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-07-21": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-07-21T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-07-21T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-07-21T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-07-28": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-07-28T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-07-28T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-07-28T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-08-04": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-08-04T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-08-04T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-08-04T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-08-11": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-08-11T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-08-11T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-08-11T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-08-18": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-08-18T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-08-18T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-08-18T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-08-25": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-08-25T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-08-25T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-08-25T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-09-01": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-09-01T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-09-01T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-09-01T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-09-08": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-09-08T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-09-08T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-09-08T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-10-15": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-10-15T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-10-15T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-10-15T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-10-22": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-10-22T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-10-22T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-10-22T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-10-29": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-10-29T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-10-29T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-10-29T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}, "2026-11-05": {"url": "https://www.thetrainline.com/book/results?journeySearchType=return&origin=urn%3Atrainline%3Ageneric%3Aloc%3AYAT3392gb&destination=urn%3Atrainline%3Ageneric%3Aloc%3APAD3087gb&outwardDate=2026-11-05T07%3A00%3A00&outwardDateType=departAfter&inwardDate=2026-11-05T18%3A00%3A00&inwardDateType=departAfter&selectedTab=train&splitSave=true&lang=en&transportModes%5B%5D=mixed", "extractor": "new Promise(resolve => {\n  const EXPECTED_DATE = '2026-11-05T07:00:00';\n  const DEADLINE_MS = 45000;\n  const POLL_MS = 900;\n  const start = Date.now();\n  function parsePrice(s){if(!s)return null;const m=s.match(/\u00a3\\s*([\\d,]+(?:\\.\\d+)?)/);return m?parseFloat(m[1].replace(/,/g,'')):null;}\n  function parseContainer(testId){const c=document.querySelector(`[data-test=\"${testId}\"]`);if(!c)return null;const dts=c.querySelectorAll('[data-test=\"train-results-departure-time\"]');const rows=[];dts.forEach(dt=>{let row=dt;for(let j=0;j<8;j++){if(!row.parentElement)break;row=row.parentElement;if(row.querySelector('[data-test=\"train-results-arrival-time\"]')&&row.querySelector('[data-test=\"alternative-price\"]'))break;}const depEl=row.querySelector('[data-test=\"train-results-departure-time\"]');const arrEl=row.querySelector('[data-test=\"train-results-arrival-time\"]');const priceEl=row.querySelector('[data-test=\"alternative-price\"]');const dt2=depEl?depEl.textContent:'';const at=arrEl?arrEl.textContent:'';const dm=dt2.match(/\\d{2}:\\d{2}/);const am=at.match(/\\d{2}:\\d{2}/);rows.push({dep:dm?dm[0]:null,arr:am?am[0]:null,price:parsePrice(priceEl?priceEl.textContent.trim():'')});});return rows;}\n  function isSkeleton(rows){if(!rows||!rows.length)return true;const deps=new Set(rows.map(r=>r.dep));if(deps.size<2)return true;if(rows.every(r=>r.price===88.88))return true;if(rows.every(r=>r.dep===r.arr))return true;return false;}\n  function attempt(){const params=new URL(window.location.href).searchParams;const urlDate=params.get('outwardDate');if(urlDate!==EXPECTED_DATE){if(Date.now()-start>DEADLINE_MS)return resolve({err:'wrong_date',got:urlDate});return setTimeout(attempt,POLL_MS);}const out=parseContainer('train-results-container-OUTWARD');const ret=parseContainer('train-results-container-INWARD');if(!out||!ret||out.length===0||ret.length===0||isSkeleton(out)||isSkeleton(ret)){if(Date.now()-start>DEADLINE_MS)return resolve({err:'timeout_or_skeleton',outwardSample:out&&out[0],inwardSample:ret&&ret[0]});return setTimeout(attempt,POLL_MS);}resolve({outward:out,inward:ret,outwardDate:urlDate});}\n  attempt();\n});\n"}}